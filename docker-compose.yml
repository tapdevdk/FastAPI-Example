version: '3.1'

services:

  db:
    image: postgres:14.3
    container_name: tapdevdk-fastapi-example-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: magenta
      POSTGRES_PASSWORD: magentadev
      POSTGRES_DB: magenta
    ports:
      - "5432:5432"